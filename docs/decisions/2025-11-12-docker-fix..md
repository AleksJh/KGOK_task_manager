# ADR-001: Разделение процессов сборки и запуска Docker-контейнеров для Django-приложения

**Статус:** Реализовано  
**Дата:** 12 ноября 2025  
**Автор:** Команда разработки  
**Контекст:** Django + PostgreSQL + Celery + Redis в Docker-окружении

---

## Контекст и описание проблемы

Django-приложение не запускалось в Docker-окружении из-за нескольких архитектурных проблем:

### Проблема 1: Зависимость от БД на этапе сборки образа
**Симптом:** 
```
psycopg2.OperationalError: could not translate host name "db" to address: 
Name or service not known
```

**Причина:**  
В `Dockerfile` была команда `RUN python manage.py migrate`, которая пыталась подключиться к PostgreSQL во время сборки образа. На этапе `docker build` сервисы из `docker-compose.yml` (включая `db`) недоступны — это два разных этапа жизненного цикла.

**Аналогия:**  
Это как пытаться позвонить другу, пока вы оба еще спите. Сборка образа = подготовка телефона, запуск контейнера = момент, когда вы оба проснулись и можете говорить.

---

### Проблема 2: Отсутствие миграций для кастомной модели User
**Симптом:**
```
psycopg2.errors.UndefinedTable: relation "tasks_user" does not exist
```

**Причина:**  
В проекте использовалась кастомная модель пользователя (`AUTH_USER_MODEL = "tasks.User"`), но для приложения `tasks` не существовало папки `migrations/` и начальных миграций. Django пыталась применить системные миграции (`admin`, `auth`, `sessions`), которые ссылались на несуществующую таблицу `tasks_user`.

**Цепочка зависимостей:**
```
admin.0001_initial → ссылается на AUTH_USER_MODEL
                  → tasks.User
                  → tasks_user table
                  → ❌ не существует
```

---

### Проблема 3: Volume mount затеняет entrypoint скрипт
**Симптом:**
```
OCI runtime create failed: exec: "/app/entrypoint.sh": 
stat /app/entrypoint.sh: no such file or directory
```

**Причина:**  
В `docker-compose.yml` был настроен bind mount `./:/app`, который перезаписывал содержимое `/app` в контейнере содержимым хост-директории. 

**Что происходило:**
1. ✅ **Сборка образа:** `COPY docker/web/entrypoint.sh /app/entrypoint.sh` — файл добавлен в образ
2. ❌ **Запуск контейнера:** `volumes: - ./:/app` — хост-директория монтируется в `/app`, скрывая файлы из образа
3. ❌ **Результат:** `entrypoint.sh` из образа "затеняется", так как его нет в корне проекта на хосте

**Визуализация проблемы:**
```
Docker Image (/app):          Host Directory (./):
├── entrypoint.sh ✅          ├── manage.py
├── manage.py                 ├── tasks/
├── tasks/                    ├── docker/
└── ...                       └── docker-compose.yml
                              (НЕТ entrypoint.sh!)
       ↓
   МОНТИРУЕМ
       ↓
Container (/app):
├── manage.py
├── tasks/
├── docker/
└── docker-compose.yml
(entrypoint.sh ИСЧЕЗ! ❌)
```

---

### Проблема 4: Гонка условий при запуске сервисов
**Причина:**  
`depends_on` в docker-compose просто определяет порядок запуска процессов, но не гарантирует готовность сервисов. PostgreSQL может быть "запущен" (процесс работает), но еще не готов принимать соединения.

**Проблемный сценарий:**
```
t=0s:  db запускается (postgres процесс стартует)
t=1s:  web запускается (depends_on: db выполнен)
t=1s:  web пытается подключиться к БД
       ❌ БД еще инициализируется
t=3s:  БД готова принимать соединения
       ❌ web уже упал с ошибкой
```

---

## Факторы принятия решения

* **Immutable Infrastructure**: Docker-образы должны быть независимы от окружения
* **12-Factor App принципы**: Разделение конфигурации и кода
* **Удобство разработки**: Быстрые циклы пересборки, понятные ошибки
* **Production-ready**: Предсказуемый порядок запуска, health checks
* **Django best practices**: Миграции как код, кастомные модели пользователей
* **Отказоустойчивость**: Автоматическое восстановление при временных сбоях

---

## Рассмотренные варианты решения

### Вариант 1: Оставить миграции в Dockerfile ❌
```dockerfile
RUN python manage.py migrate
```

**Плюсы:**
- Простота реализации
- Миграции выполняются один раз

**Минусы:**
- ❌ Требует БД на этапе сборки (нарушает принципы Docker)
- ❌ Ломает CI/CD pipeline (нужен доступ к тестовой БД при сборке)
- ❌ Образ привязан к конкретному окружению
- ❌ Невозможно использовать один образ для dev/staging/production

**Вердикт:** Архитектурно неверное решение

---

### Вариант 2: Ручной запуск миграций ❌
```bash
docker-compose up -d db
docker-compose exec web python manage.py migrate
docker-compose up -d
```

**Плюсы:**
- Полный контроль над процессом
- Подходит для разработки

**Минусы:**
- ❌ Не автоматизировано
- ❌ Требует документации и дисциплины команды
- ❌ Подвержено человеческим ошибкам
- ❌ Не подходит для production (нет автоматического восстановления)

**Вердикт:** Не масштабируемое решение

---

### Вариант 3: Runtime-инициализация через entrypoint ✅
```bash
entrypoint.sh:
  wait_for_db()
  migrate()
  collectstatic()
  exec $CMD
```

**Плюсы:**
- ✅ Чистое разделение build/runtime
- ✅ Работает в любом окружении
- ✅ Идемпотентность (можно запускать повторно)
- ✅ Автоматическое восстановление при перезапуске
- ✅ Следует cloud-native принципам

**Минусы:**
- Небольшая задержка при старте (2-5 секунд ожидания БД)
- Требует правильной настройки health checks

**Вердикт:** ✅ **ВЫБРАНО** — баланс между надежностью и удобством

---

## Принятое решение

**Выбрано:** Runtime-инициализация через интеллектуальный entrypoint-скрипт с оркестрацией через health checks

---

## Детали реализации

### 1. Изменения в Dockerfile

#### Было (❌ нерабочее):
```dockerfile
# Попытка мигрировать на этапе сборки
RUN python manage.py migrate  

CMD ["gunicorn", "config.wsgi:application"]
```

**Проблема:** БД недоступна на этапе `docker build`

---

#### Стало (✅ рабочее):
```dockerfile
# Копируем и делаем исполняемым entrypoint
COPY docker/web/entrypoint.sh /app/entrypoint.sh
RUN chmod +x /app/entrypoint.sh

# Устанавливаем точку входа
ENTRYPOINT ["/app/entrypoint.sh"]

# CMD передается как аргументы в entrypoint
CMD ["gunicorn", "config.wsgi:application", "--bind", "0.0.0.0:8000"]
```

**Преимущества:**
- Миграции перенесены на runtime
- Образ можно собрать без БД
- Один образ работает в любом окружении

---

### 2. Entrypoint-скрипт (`docker/web/entrypoint.sh`)
```bash
#!/bin/sh
set -e  # Останавливаем при любой ошибке

echo "🔍 Ожидание БД ${POSTGRES_HOST}:${POSTGRES_PORT}..."

# Умное ожидание готовности БД с retry-логикой
until python -c "
import psycopg2
import os
conn = psycopg2.connect(
    host=os.getenv('POSTGRES_HOST', 'db'),
    port=os.getenv('POSTGRES_PORT', '5432'),
    user=os.getenv('POSTGRES_USER', 'postgres'),
    password=os.getenv('POSTGRES_PASSWORD', ''),
    dbname=os.getenv('POSTGRES_DB', 'postgres')
)
conn.close()
" 2>/dev/null; do
  echo "⏳ БД недоступна, повтор через 2 секунды..."
  sleep 2
done

echo "✅ БД доступна!"

# Условное выполнение миграций (только для web-сервиса)
if [ "$MIGRATE_ON_START" = "true" ]; then
  echo "🔄 Выполняю миграции..."
  python manage.py migrate --noinput
  echo "✅ Миграции применены"
fi

# Условный сбор статики (заполнение volume)
if [ "$COLLECTSTATIC_ON_START" = "true" ]; then
  echo "📦 Собираю статику..."
  python manage.py collectstatic --noinput --clear
  echo "✅ Статика собрана"
fi

# Передаем управление основной команде (CMD из Dockerfile)
echo "🚀 Запуск приложения: $@"
exec "$@"
```

**Ключевые особенности:**

1. **Активное ожидание БД** с использованием реального подключения (не просто проверка порта)
2. **Условное выполнение миграций** через переменную окружения `MIGRATE_ON_START`
3. **Идемпотентность**: можно запускать многократно, Django сама определит нужные миграции
4. **Прозрачность**: детальное логирование каждого этапа
5. **Fail-fast**: `set -e` останавливает при первой ошибке

---

### 3. Изменения в docker-compose.yml

#### Было (❌ проблемное):
```yaml
services:
  web:
    volumes:
      - ./:/app  # ❌ Затеняет entrypoint.sh
    depends_on:
      - db      # ❌ Только порядок запуска, не готовность
```

---

#### Стало (✅ правильное):
```yaml
services:
  db:
    image: postgres:15-alpine
    # Проверка готовности БД
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 5s      # Проверка каждые 5 секунд
      timeout: 3s       # Таймаут проверки
      retries: 5        # Количество попыток
      start_period: 10s # Время на первичную инициализацию

  web:
    build:
      context: .
      dockerfile: docker/web/Dockerfile
    volumes:
      # ✅ Только данные, не код!
      - static_volume:/app/staticfiles
      - media_volume:/app/media
      # ❌ Убран: - ./:/app
    environment:
      - MIGRATE_ON_START=true           # ✅ Web выполняет миграции
      - COLLECTSTATIC_ON_START=true     # ✅ Web собирает статику
    depends_on:
      db:
        condition: service_healthy      # ✅ Ждем ГОТОВНОСТИ, не запуска
      redis:
        condition: service_healthy

  worker:
    build:
      context: .
      dockerfile: docker/web/Dockerfile
    command: celery -A config worker -l info
    environment:
      - MIGRATE_ON_START=false          # ✅ Worker НЕ мигрирует
      - COLLECTSTATIC_ON_START=false    # ✅ Worker НЕ собирает статику
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      web:
        condition: service_started       # ✅ После web (который мигрировал)

volumes:
  static_volume:    # Именованный том для статики
  media_volume:     # Именованный том для медиа
```

**Критически важные изменения:**

1. **Health checks для БД**: `pg_isready` проверяет реальную готовность PostgreSQL
2. **`condition: service_healthy`**: Сервисы стартуют только после готовности зависимостей
3. **Убраны bind mounts кода**: `./:/app` удален, теперь код живет в образе
4. **Именованные тома**: статика и медиа в отдельных volumes
5. **Флаги MIGRATE_ON_START**: разделение ответственности между сервисами

---

### 4. Создание начальных миграций

**Проблема:** Приложение `tasks` не имело папки `migrations/`

**Решение:** Создана начальная миграция `tasks/migrations/0001_initial.py`

#### Структура миграции:
```python
# tasks/migrations/0001_initial.py
from django.db import migrations, models
import django.contrib.auth.validators

class Migration(migrations.Migration):
    initial = True
    
    dependencies = [
        ('auth', '0012_alter_user_first_name_max_length'),
    ]
    
    operations = [
        # 1. Department (независимая модель)
        migrations.CreateModel(
            name='Department',
            fields=[
                ('id', models.BigAutoField(primary_key=True)),
                ('name', models.CharField(max_length=100)),
                ('email', models.EmailField(unique=True)),
            ],
        ),
        
        # 2. User (зависит от Department и auth)
        migrations.CreateModel(
            name='User',
            fields=[
                ('id', models.BigAutoField(primary_key=True)),
                ('password', models.CharField(max_length=128)),
                ('username', models.CharField(
                    max_length=150, 
                    unique=True,
                    validators=[django.contrib.auth.validators.UnicodeUsernameValidator()]
                )),
                ('is_admin', models.BooleanField(default=False)),
                ('department', models.ForeignKey(
                    to='tasks.department',
                    on_delete=models.SET_NULL,
                    null=True,
                    blank=True
                )),
                # ... другие поля AbstractUser
            ],
            options={'db_table': 'tasks_user'},
            managers=[('objects', django.contrib.auth.models.UserManager())],
        ),
        
        # 3. Task (зависит от User и Department)
        migrations.CreateModel(name='Task', ...),
        
        # 4. Comment (зависит от Task и User)
        migrations.CreateModel(name='Comment', ...),
    ]
```

**Порядок создания таблиц критичен:**
```
1. Department (независимая)
   ↓
2. User (FK → Department)
   ↓
3. Task (FK → User, FK → Department)
   ↓
4. Comment (FK → Task, FK → User)
```

---

## Последовательность запуска системы

### Правильная последовательность событий:
```
1. docker-compose up -d
   ↓
2. 🗄️ PostgreSQL стартует
   ├─ Процесс запущен (t=0s)
   ├─ Инициализация БД (t=1-5s)
   ├─ pg_isready = healthy (t=5s) ✅
   └─ Готов к соединениям
   ↓
3. 🌐 Web-контейнер стартует
   ├─ depends_on: db (healthy) выполнен ✅
   ├─ entrypoint.sh начинает работу
   ├─ Ожидание БД (цикл с retry)
   ├─ БД доступна ✅
   ├─ MIGRATE_ON_START=true → выполнение миграций
   │  ├─ tasks.0001_initial
   │  ├─ admin.0001_initial
   │  └─ auth, contenttypes, sessions...
   ├─ COLLECTSTATIC_ON_START=true → сбор статики
   └─ exec gunicorn → запуск приложения ✅
   ↓
4. 👷 Worker-контейнер стартует
   ├─ depends_on: db (healthy), web (started) ✅
   ├─ entrypoint.sh начинает работу
   ├─ Ожидание БД
   ├─ MIGRATE_ON_START=false → пропуск миграций ⏭️
   └─ exec celery worker ✅
   ↓
5. 🚀 Система готова к работе
```

**Timing diagram:**
```
t=0s   [db] Starting...
t=2s   [db] Initializing...
t=5s   [db] Healthy ✅
       [web] Starting (waiting for db...)
t=6s   [web] DB available, running migrations...
t=10s  [web] Migrations done, collecting static...
t=12s  [web] Gunicorn started ✅
       [worker] Starting (waiting for db...)
t=13s  [worker] DB available, skipping migrations
t=14s  [worker] Celery started ✅
```

---

## Последствия решения

### ✅ Положительные

1. **Надежность**
   - Автоматическое восстановление при временных сбоях БД
   - Идемпотентные миграции (можно запускать повторно)
   - Явная проверка готовности зависимостей

2. **Переносимость**
   - Один образ работает в dev/staging/production
   - Не требует БД на этапе CI/CD
   - Конфигурация через переменные окружения

3. **Простота разработки**
   - Понятные логи каждого этапа инициализации
   - Быстрая диагностика проблем
   - `docker-compose up -d` — и всё работает

4. **Production-ready**
   - Health checks для мониторинга
   - Graceful startup (корректное ожидание зависимостей)
   - Разделение ответственности (web мигрирует, worker нет)

5. **Соответствие best practices**
   - 12-Factor App (конфигурация vs код)
   - Immutable Infrastructure
   - Infrastructure as Code

---

### ⚠️ Компромиссы

1. **Время запуска**
   - Задержка 5-15 секунд на ожидание БД и миграции
   - **Приемлемо:** происходит только при старте/рестарте

2. **Сложность entrypoint**
   - Дополнительный скрипт для поддержки
   - **Компенсируется:** подробные комментарии и логирование

3. **Требования к docker-compose версии**
   - `depends_on.condition` требует Compose v2.1+
   - **Решение:** Docker Desktop на Windows уже поддерживает

---

### 📊 Сравнение "до" и "после"

| Аспект | До ❌ | После ✅ |
|--------|------|---------|
| **Сборка образа** | Требует БД | Независимая |
| **Запуск в новом окружении** | Ручные шаги | Автоматический |
| **Устойчивость к сбоям** | Падает при недоступности БД | Автоматический retry |
| **Миграции** | Отсутствуют для tasks | Полный набор |
| **Монтирование кода** | Bind mount затеняет файлы | Только данные в volumes |
| **Порядок запуска** | Гонка условий | Явные зависимости + health checks |
| **Дублирование миграций** | Риск (web и worker) | Защита через MIGRATE_ON_START |
| **Отладка** | Непонятные ошибки | Детальное логирование |

---

## Альтернативы и будущие улучшения

### Рассмотренные альтернативы

1. **wait-for-it.sh / dockerize**
   - Популярные утилиты для ожидания сервисов
   - **Не выбрано:** дополнительные зависимости, наш скрипт проще и специфичнее

2. **Kubernetes Init Containers**
   - Нативный механизм K8s для инициализации
   - **Не актуально:** проект пока на docker-compose

3. **Django management команда для wait_for_db**
   - Встроить логику в Django
   - **Не выбрано:** entrypoint универсальнее (работает с любыми командами)

---

### Возможные улучшения

1. **Для разработки: hot-reload**
```yaml
   # docker-compose.override.yml (только для dev)
   services:
     web:
       volumes:
         - ./:/app  # Вернуть bind mount
         - /app/entrypoint.sh  # Исключить entrypoint
```

2. **Расширенный health check для Django**
```dockerfile
   HEALTHCHECK --interval=30s --timeout=3s \
     CMD python -c "import requests; requests.get('http://localhost:8000/health/')"
```

3. **Метрики времени инициализации**
```bash
   # В entrypoint.sh
   START_TIME=$(date +%s)
   # ... миграции ...
   END_TIME=$(date +%s)
   echo "⏱️ Инициализация заняла $((END_TIME - START_TIME)) секунд"
```

4. **Backup перед миграциями**
```bash
   if [ "$AUTO_BACKUP" = "true" ]; then
     pg_dump > /backups/pre-migration-$(date +%s).sql
   fi
```

---

## Как использовать

### Первый запуск проекта
```bash
# 1. Создать .env файл
cp .env.example .env

# 2. Убедиться, что POSTGRES_HOST=db (должно быть в .env)
# 3. Собрать образы
docker-compose build --no-cache

# 4. Запустить все сервисы
docker-compose up -d

# 5. Проверить логи
docker-compose logs -f web
docker-compose logs -f worker
```

**Ожидаемый вывод web:**
```
🔍 Ожидание БД db:5432...
✅ БД доступна!
🔄 Выполняю миграции...
Operations to perform:
  Apply all migrations: admin, auth, contenttypes, sessions, tasks
Running migrations:
  Applying tasks.0001_initial... OK
  Applying admin.0001_initial... OK
  ...
✅ Миграции применены
📦 Собираю статику...
✅ Статика собрана
🚀 Запуск приложения: gunicorn config.wsgi:application --bind 0.0.0.0:8000
```

---

### Применение новых миграций
```bash
# 1. Создать миграцию (внутри контейнера)
docker-compose run --rm -e MIGRATE_ON_START=false web \
  python manage.py makemigrations

# 2. Скопировать файл миграции из контейнера (если нет bind mount)
docker cp <container_id>:/app/tasks/migrations/0002_new_migration.py \
  ./tasks/migrations/

# 3. Пересобрать образ (включить новую миграцию)
docker-compose build web

# 4. Перезапустить (миграция применится автоматически)
docker-compose up -d web
```

---

### Откат при проблемах
```bash
# 1. Остановить все сервисы
docker-compose down

# 2. Удалить volume с БД (ВНИМАНИЕ: потеря данных!)
docker volume rm kgok_task_manager_postgres_data

# 3. Пересоздать всё с нуля
docker-compose up -d
```

---

## Проверка работоспособности

### Health Check Checklist

- [ ] `docker-compose ps` — все сервисы в состоянии `healthy` или `running`
- [ ] `docker-compose logs db` — нет ошибок PostgreSQL
- [ ] `docker-compose logs web` — миграции применены, Gunicorn запущен
- [ ] `docker-compose logs worker` — Celery подключился к Redis и БД
- [ ] `curl http://localhost:8000/` — приложение отвечает
- [ ] `docker-compose exec web python manage.py showmigrations` — все миграции applied

### Команды диагностики
```bash
# Проверить здоровье сервисов
docker-compose ps

# Логи конкретного сервиса с таймстампами
docker-compose logs -f --tail=100 web

# Зайти внутрь контейнера
docker-compose exec web sh

# Проверить миграции
docker-compose exec web python manage.py showmigrations

# Проверить подключение к БД
docker-compose exec web python manage.py dbshell

# Проверить переменные окружения
docker-compose exec web env | grep POSTGRES
```

---

## Уроки и выводы

### Что мы узнали

1. **Docker build ≠ Docker run**
   - Сборка образа — статическая операция, без сетевого взаимодействия
   - Запуск контейнера — динамический процесс с доступом к сети и БД

2. **Volume mounts затеняют образ**
   - Bind mount `./:/app` перезаписывает всё содержимое `/app` из образа
   - Используйте именованные volumes для данных, а не для кода

3. **depends_on — это не health check**
   - `depends_on: [db]` ≠ "БД готова"
   - Нужен явный `healthcheck` + `condition: service_healthy`

4. **Entrypoint — это оркестратор**
   - Не просто запуск команды, а интеллектуальная инициализация
   - Правильный entrypoint = надежность системы

5. **Миграции Django идемпотентны**
   - Можно запускать многократно без вреда
   - Django сама отслеживает, что уже применено

6. **Кастомные модели User требуют миграций ДО системных**
   - `AUTH_USER_MODEL` должен быть мигрирован в первую очередь
   - Системные приложения (`admin`, `auth`) зависят от него

---

### Принципы для будущих проектов

1. ✅ **Разделяй build и runtime** — никаких внешних зависимостей при сборке
2. ✅ **Используй health checks** — явная проверка готовности сервисов
3. ✅ **Создавай умные entrypoints** — автоматизация инициализации
4. ✅ **Минимизируй bind mounts** — монтируй только данные, не код
5. ✅ **Документируй архитектурные решения** — ADR помогает команде понимать "почему"
6. ✅ **Делай миграции частью репозитория** — version control для схемы БД
7. ✅ **Логируй каждый этап** — упрощает диагностику проблем
8. ✅ **Используй переменные окружения для флагов** — гибкость без изменения кода

---

## Команды для разработчиков

### Шпаргалка по Docker Compose
```bash
# === ЗАПУСК И ОСТАНОВКА ===
docker-compose up -d              # Запустить все сервисы в фоне
docker-compose up web             # Запустить только web (+ зависимости)
docker-compose down               # Остановить и удалить контейнеры
docker-compose down -v            # + удалить volumes (БД будет очищена!)
docker-compose restart web        # Перезапустить сервис

# === СБОРКА ===
docker-compose build              # Пересобрать изменённые образы
docker-compose build --no-cache   # Полная пересборка без кеша
docker-compose build web          # Пересобрать только web

# === ЛОГИ И МОНИТОРИНГ ===
docker-compose logs -f            # Логи всех сервисов в реальном времени
docker-compose logs -f web        # Логи только web
docker-compose logs --tail=50 web # Последние 50 строк
docker-compose ps                 # Статус всех сервисов
docker-compose top                # Процессы внутри контейнеров

# === ВЫПОЛНЕНИЕ КОМАНД ===
docker-compose exec web sh                    # Войти в shell контейнера
docker-compose exec web python manage.py shell # Django shell
docker-compose exec web python manage.py makemigrations
docker-compose exec web python manage.py migrate
docker-compose exec db psql -U postgres       # PostgreSQL клиент

# === ОДНОРАЗОВЫЕ КОМАНДЫ ===
docker-compose run --rm web python manage.py createsuperuser
docker-compose run --rm -e MIGRATE_ON_START=false web python manage.py test

# === ОТЛАДКА ===
docker-compose exec web env                   # Переменные окружения
docker-compose exec web cat /app/entrypoint.sh # Проверить entrypoint
docker-compose config                         # Валидация docker-compose.yml
```

---

## Troubleshooting Guide

### Проблема: Контейнер web постоянно перезапускается

**Симптомы:**
```bash
$ docker-compose ps
NAME                    STATUS
kgok_task_manager-web-1 Restarting
```

**Диагностика:**
```bash
docker-compose logs web --tail=50
```

**Возможные причины:**

1. **Миграции падают с ошибкой**
```
   Решение: Проверить логи миграций, возможно проблема в models.py
```

2. **БД недоступна (healthcheck не работает)**
```bash
   # Проверить статус БД
   docker-compose exec db pg_isready -U postgres
   
   # Если не отвечает — перезапустить БД
   docker-compose restart db
```

3. **Неправильные переменные окружения**
```bash
   # Проверить .env файл
   cat .env | grep POSTGRES
   
   # Должно быть:
   POSTGRES_HOST=db  # НЕ localhost!
```

---

### Проблема: Ошибка "entrypoint.sh not found"

**Симптомы:**
```
exec: "/app/entrypoint.sh": stat /app/entrypoint.sh: no such file or directory
```

**Причина:** Bind mount затеняет файлы из образа

**Решение:**
```yaml
# В docker-compose.yml убедитесь, что НЕТ:
volumes:
  - ./:/app  # ❌ УДАЛИТЬ!

# Должны быть только:
volumes:
  - static_volume:/app/staticfiles  # ✅
  - media_volume:/app/media          # ✅
```

---

### Проблема: Миграции не применяются

**Симптомы:**
```bash
$ docker-compose exec web python manage.py showmigrations
[ ] tasks.0001_initial  # Не применена!
```

**Диагностика:**
```bash
# Проверить флаг MIGRATE_ON_START
docker-compose exec web env | grep MIGRATE_ON_START

# Должно быть:
MIGRATE_ON_START=true  # Для web
```

**Ручное применение:**
```bash
docker-compose exec web python manage.py migrate --noinput
```

---

### Проблема: Статика не отдаётся

**Симптомы:** CSS/JS не загружаются, 404 на `/static/`

**Причина:** Статика не собрана в volume

**Решение:**
```bash
# Проверить наличие файлов
docker-compose exec web ls -la /app/staticfiles/

# Если пусто — собрать вручную
docker-compose exec web python manage.py collectstatic --noinput

# Проверить volume в nginx
docker-compose exec nginx ls -la /static/
```

---

### Проблема: Worker не видит задачи Celery

**Симптомы:** Задачи не выполняются, очередь пустая

**Диагностика:**
```bash
# Проверить подключение worker к Redis
docker-compose logs worker | grep "Connected to redis"

# Проверить задачи в Django shell
docker-compose exec web python manage.py shell
>>> from tasks.tasks import some_task
>>> result = some_task.delay()
>>> result.status
```

**Решение:**
```bash
# Перезапустить worker
docker-compose restart worker

# Проверить, что Redis работает
docker-compose exec redis redis-cli ping
# Должен ответить: PONG
```

---

### Проблема: "table already exists" при миграциях

**Симптомы:**
```
django.db.utils.ProgrammingError: relation "tasks_user" already exists
```

**Причина:** Миграция пытается создать таблицу, которая уже есть

**Решение:**
```bash
# Вариант 1: Fake-миграция (если структура совпадает)
docker-compose exec web python manage.py migrate tasks --fake

# Вариант 2: Полный сброс БД (ВНИМАНИЕ: потеря данных!)
docker-compose down -v
docker-compose up -d

# Вариант 3: Ручная корректировка миграций
# Отредактировать файл миграции, убрать проблемные операции
```

---

## FAQ (Часто задаваемые вопросы)

### В: Почему миграции в entrypoint, а не в Dockerfile?

**О:** На этапе `docker build` сервисы из `docker-compose.yml` недоступны. База данных существует только при `docker-compose up`. Entrypoint выполняется на этапе runtime, когда все сервисы запущены.

---

### В: Почему worker не запускает миграции?

**О:** Миграции должны выполняться один раз, в одном месте (web). Worker использует уже мигрированную схему. Это предотвращает конфликты и дублирование работы.

---

### В: Можно ли вернуть bind mount для разработки?

**О:** Да, создайте `docker-compose.override.yml`:
```yaml
# docker-compose.override.yml (автоматически применяется)
services:
  web:
    volumes:
      - ./:/app
      - /app/entrypoint.sh  # Исключаем из bind mount
    environment:
      - DEBUG=true
```

**Но:** это затрудняет отладку проблем с entrypoint. Рекомендуется для production использовать образ без bind mount.

---

### В: Как добавить новую миграцию?

**О:**
```bash
# 1. Изменить models.py
# 2. Создать миграцию
docker-compose run --rm -e MIGRATE_ON_START=false web \
  python manage.py makemigrations

# 3. Если нет bind mount — скопировать файл из контейнера
docker-compose run --rm -e MIGRATE_ON_START=false web \
  python manage.py makemigrations > /tmp/migration_output.txt

# 4. Пересобрать образ
docker-compose build web

# 5. Применить миграцию (автоматически при запуске)
docker-compose up -d web
```

---

### В: Как ускорить запуск контейнеров?

**О:** Время запуска складывается из:
- Ожидание БД: 2-5 секунд (зависит от health check interval)
- Миграции: 1-10 секунд (зависит от количества)
- Collectstatic: 1-5 секунд

**Оптимизация:**
```yaml
# Сократить интервал health check (не рекомендуется < 3s)
healthcheck:
  interval: 3s  # Вместо 5s
  
# Отключить collectstatic если статика не меняется
environment:
  - COLLECTSTATIC_ON_START=false
```

**Но:** эти секунды обеспечивают надёжность. Не жертвуйте стабильностью ради скорости.

---

### В: Что делать если healthcheck не поддерживается?

**О:** Если версия docker-compose < 2.1:
```yaml
# Убрать условия depends_on
depends_on:
  - db  # Простая зависимость без condition

# Entrypoint всё равно подождёт БД благодаря retry-логике
```

---

### В: Как проверить, что всё работает правильно?

**О:** Финальная проверка:
```bash
# 1. Чистый запуск
docker-compose down -v
docker-compose up -d

# 2. Проверить логи (не должно быть ошибок)
docker-compose logs

# 3. Проверить статус (всё должно быть Up)
docker-compose ps

# 4. Проверить миграции (всё [X])
docker-compose exec web python manage.py showmigrations

# 5. Проверить приложение
curl http://localhost:8000/

# 6. Создать тестовую задачу
docker-compose exec web python manage.py shell
>>> from tasks.models import Task
>>> Task.objects.count()  # Должно работать
```

---

## Метрики и мониторинг

### Сбор метрик времени инициализации

Можно добавить в `entrypoint.sh`:
```bash
#!/bin/sh
set -e

# Записываем время старта
INIT_START=$(date +%s)

echo "🔍 Ожидание БД..."
DB_WAIT_START=$(date +%s)
# ... wait for db ...
DB_WAIT_END=$(date +%s)
echo "⏱️  БД ожидание: $((DB_WAIT_END - DB_WAIT_START))s"

if [ "$MIGRATE_ON_START" = "true" ]; then
  MIGRATE_START=$(date +%s)
  python manage.py migrate --noinput
  MIGRATE_END=$(date +%s)
  echo "⏱️  Миграции: $((MIGRATE_END - MIGRATE_START))s"
fi

if [ "$COLLECTSTATIC_ON_START" = "true" ]; then
  STATIC_START=$(date +%s)
  python manage.py collectstatic --noinput --clear
  STATIC_END=$(date +%s)
  echo "⏱️  Статика: $((STATIC_END - STATIC_START))s"
fi

INIT_END=$(date +%s)
echo "⏱️  Общее время инициализации: $((INIT_END - INIT_START))s"

exec "$@"
```

**Пример вывода:**
```
⏱️  БД ожидание: 3s
⏱️  Миграции: 7s
⏱️  Статика: 2s
⏱️  Общее время инициализации: 12s
```

---

## Связанные документы

- [Django Deployment Checklist](https://docs.djangoproject.com/en/stable/howto/deployment/checklist/)
- [Docker Compose File Reference](https://docs.docker.com/compose/compose-file/)
- [PostgreSQL in Docker](https://hub.docker.com/_/postgres)
- [12-Factor App Methodology](https://12factor.net/)
- [Health Check Best Practices](https://docs.docker.com/engine/reference/builder/#healthcheck)

---

## История изменений

| Дата | Версия | Изменения | Автор |
|------|--------|-----------|-------|
| 2025-11-12 | 1.0 | Первая версия ADR, описание архитектурного решения | Команда разработки |

---

## Заключение

Проделанная работа трансформировала проект из "работает у меня на машине" в production-ready систему с:

- ✅ **Надёжным запуском** — автоматическое ожидание зависимостей
- ✅ **Портируемостью** — один образ для всех окружений
- ✅ **Отказоустойчивостью** — retry-логика и health checks
- ✅ **Прозрачностью** — детальное логирование каждого этапа
- ✅ **Масштабируемостью** — правильная архитектура для роста

Этот подход является стандартом для современных cloud-native приложений и может быть применён к другим проектам с минимальными изменениями.

**Ключевой принцип:** Разделение этапов build и runtime делает систему более надёжной, понятной и поддерживаемой.

---

## Контакты и поддержка

Если у вас возникли вопросы по этому архитектурному решению:

1. Проверьте раздел **Troubleshooting Guide**
2. Просмотрите **FAQ**
3. Изучите логи контейнеров: `docker-compose logs`
4. Создайте issue с описанием проблемы и приложенными логами

**Полезные команды для диагностики:**
```bash
# Полная диагностика системы
docker-compose ps
docker-compose logs --tail=100
docker-compose exec web python manage.py check
docker-compose exec db pg_isready
```

---

**Версия документа:** 1.0  
**Последнее обновление:** 12 ноября 2025  
**Статус:** Активный / Используется в production